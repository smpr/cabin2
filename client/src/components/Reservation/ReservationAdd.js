import React, { Component } from 'react'

export default class ReservationAdd extends Component {
  render() {
    return (
      <div>
        For creating a reservation on the callendar. Needs to check logic of current list to see if its in use yet. 
      </div>
    )
  }
}
